#include <thread>
#include <iostream>
#include <vector>
#include <cstdlib>
#include <ctime>
#include <chrono>
#include <thread>

using namespace std;


int main(int argc, char** argv)
{
	size_t vecSize = 0;
	if(argc != 2)
	{
		cout << "Неверное число аргументов! Укажите число элементов!" << endl;
		return 0;
	}
  return 0;
}
